# Codex Working Notes

This file is for durable, lightweight project memory between coding sessions. Keep entries short and factual.

## Current Working Tree Notes

- Created on 2026-05-07 to give Codex a compact project context.
- At creation time, `git status --short` showed existing changes in `public/style.css` and several untracked folders/files, including `public/css/`, `notebooks/`, and `scratch/`. Treat those as user or previous-session work unless proven otherwise.

## Update Guidelines

- Add notes only when they will help a future session avoid rediscovery.
- Prefer concrete file paths and behavior over broad summaries.
- Do not store secrets, tokens, passwords, or private credentials here.
- Do not use this as a task tracker; use it for stable context and decisions.

